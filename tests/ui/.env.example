# Copy this file to `.env` and adjust it for your local bench.
PLAYWRIGHT_BASE_URL=http://localhost:8000

# Used by tests/ui/auth.setup.ts to create the cached browser session.
FRAPPE_TEST_USER=Administrator
FRAPPE_TEST_PASSWORD=admin
